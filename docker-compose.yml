version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: zorbit-audit-db
    environment:
      POSTGRES_DB: zorbit_audit
      POSTGRES_USER: zorbit
      POSTGRES_PASSWORD: zorbit_dev
    ports:
      - '5438:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U zorbit -d zorbit_audit']
      interval: 5s
      timeout: 5s
      retries: 5

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zorbit-audit-zk
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - '2186:2181'

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: zorbit-audit-kafka
    depends_on:
      - zookeeper
    ports:
      - '9097:9092'
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9097
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'

volumes:
  pgdata:
